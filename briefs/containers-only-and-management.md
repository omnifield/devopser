# Brief — Containers-only: тачка = Docker + файлы; Desktop-независимое управление

| | |
|---|---|
| **Адресат** | devopser-архитектор |
| **От** | оракул-архитектор, 2026-07-10 (решения user; канон — knowledger `principles/containers-only-execution.md`) |
| **Заменяет** | амендмент-хвост в `docker-dev-environment.md` (тот бриф остаётся историей devbox D1/D2; исполнение — по ЭТОМУ) |

## Фундамент (user, P0)

1. **Всё исполняется ТОЛЬКО в контейнере.** Нет Docker на машине — нет работы.
   Хост-путь не проектируем и не поддерживаем.
2. **На тачке — только Docker и файлы. Тачку не трогаем вообще**: ни git, ни node/uv/go,
   ни Claude Code на хосте. Весь движняк — git-операции (клон изнутри контейнера в
   примонтированную папку), Claude-сессии, тулчейн, сервисы — в докере.
3. **Файлы на машине**: bind-mount workspace (Windows — клон в WSL2 FS). Контейнер
   монтирует только рабочую папку.
4. **Docker Desktop = вопрос интерфейса, не движка**: докер обязателен (способ установки
   любой, Desktop ок), но управление/мониторинг не должны зависеть от Desktop-UI —
   его есть не везде.

## Деливераблы

### D1 — Правки существующих артефактов под фундамент
- devbox-шаблон: дефолт = **bind-mount** рабочей копии (clone-in-volume — fallback по
  перф-замеру); README — «Windows: клон в WSL2 FS».
- **workstation-bootstrap пересмотреть**: базовый слой машины = Docker, точка
  (нынешний git+node+pnpm+uv+claude набор на хост — отменяется; гайд установки Docker
  per-платформа: Windows Desktop ИЛИ engine в WSL2 — оба валидны; серверы — engine).
- Доки/README, где упоминается хост-тулчейн, — в актуал.

### D2 — Desktop-независимое управление (боль user: «не могу нормально управлять»)
1. **docker contexts** — нативное управление удалёнными демонами по SSH из одного CLI
   (ноль инфры) — задокументировать первым инструментом.
2. **Portainer CE (self-hosted, free)** — web-пульт локальных + удалённых демонов
   (agents на серверах): оценка → если ок, стек-деливерабл. Это «монитор удалённых
   серверов» уровня ИНФРЫ (докер-хосты/контейнеры); наблюдаемость агент-сессий
   остаётся продуктом brainer — границу не смешивать.

### D3 — GPU-проверка (гейт перед llm-engine)
nvidia container toolkit + `--gpus all` в WSL2-докере — проверить ДО прихода моего
порта llm-engine. Сам заказ compose (llm-engine CUDA-контейнер + polygon-devbox в одной
сети, контракт env `LLM_ENGINE_URL`) станет конкретным с портом — я сообщу состав;
Dockerfile пакета мой (граница CC-11), сеть/GPU/volume весов — твои.

### D4 — Blueprint: Claude-сессии в контейнере как ДЕФОЛТ рабочего флоу
Раз тачку не трогаем — в контейнере живут и наши рабочие Claude-сессии, не только
полигон. Проработать (не молча): креды/keychain и их персистентность между рестартами,
harness-хуки (.claude), git-идентичность/подпись из контейнера, доступ к докер-демону
изнутри (для управления стеками из сессии — socket-проброс и его безопасность).
Свериться с ⭐-секцией фидбека devbox-теста brainer — там первые ответы. Показ user+мне.

## Порядок
D1 (правки) → D2.1 (contexts) → D3 (GPU-гейт, может параллельно) → D2.2 (Portainer
оценка) → D4 (blueprint). Находки — комментарием сюда, эскалация через user.

---

## ✅ Триаж + исполнение devopser-architect (2026-07-10)

- **D1 — готово.** `workstation/bootstrap.ps1` переписан: ставит/верифицирует ровно
  Docker (в `-Verify` добавлена проверка живости engine, не только бинаря); README
  workstation — containers-only (таблица «хост/контейнер», пост-шаги переехали
  внутрь контейнера); `devbox/README` — bind-mount дефолт, clone-in-volume fallback;
  хост-тулчейн-упоминания вычищены из ARCHITECTURE/CLAUDE/repos.md.
  ⚠️ Следствие: e2e-гейт workstation (старый Q2, эскалация bootstrap-gaps)
  переопределён — DoD теперь «Docker ставится + engine отвечает», прогон на чистой
  среде стал тривиальнее.
- **D2.1 — готово.** `workstation/docker.md`: установка per-платформа (Desktop /
  engine-в-WSL2 / сервер) + `docker context` как первый инструмент управления
  удалёнными демонами (примеры). Portainer (D2.2) — отдельная оценка, не тащим до неё.
- **D3 — прогнан, вердикт: GPU есть, прокидка сломана, чинится у нас.**
  Референс-тачка: RTX 3070 Ti, драйвер 591.86, nvidia-хук в Docker Desktop есть,
  но контейнер падает: `libnvidia-ml.so.1` не находится в WSL — звено
  «драйвер инжектит библиотеки в WSL» не отработало. Первый кандидат — `wsl --update`
  (обновление системного компонента — по отмашке user). Гейт сработал как задуман:
  поймали ДО прихода llm-engine. Граница «GPU нужен только llm-engine-контейнеру,
  devbox/сервисы не касаются» зафиксирована в docker.md.
- **D2.2 — оценка ✅, стек поставлен.** Portainer CE против docker contexts: contexts
  закрывает CLI (разовые команды, скрипты), Portainer добавляет то, чего CLI не даёт —
  живой web-обзор (контейнеры/логи/стеки/volumes) локального и удалённых демонов;
  self-hosted, free, один контейнер — стоимость минимальна, self-host-first не
  нарушен. Вердикт: берём как ОПЦИОНАЛЬНЫЙ пульт. Деливерабл: `stacks/portainer/`
  (первый needs-driven стек, заказчик — user; CE 2.39.4 запинен, порт 9443 в
  ports.md, socket локально, наружу не публикуем). Smoke: up + HTTP 200 — зелёный.
  Граница с brainer-наблюдаемостью зафиксирована в README стека.
- **D3, статус починки (диагноз полный):** корень — **Docker Desktop работал на
  Hyper-V/linuxkit-бэкенде, не на WSL2** (ядро `6.10.14-linuxkit`; GPU-прокидка
  работает только через WSL2). Сам WSL на машине был сломан
  (`REGDB_E_CLASSNOTREG` — потому Desktop и жил на Hyper-V); вылечен установкой
  WSL 2.7.10 через `winget install Microsoft.WSL` (админ). Драйверные либы на
  хосте есть (`System32/lxss/lib`). `WslEngineEnabled=true` выставлен в
  settings-store.json Desktop (бэкап рядом). Остался один шаг —
  **перезагрузка машины** (Virtual Machine Platform включён установщиком WSL,
  требует ребут), после неё — контрольный прогон гейта.

---

## 📐 Blueprint D4 — Claude-сессии в контейнере как дефолт (ЧЕРНОВИК v0, 2026-07-10)

Финализируется после ⭐-секции фидбека devbox-brainer (агент-вердикт ещё не пришёл);
показ user+оракулу — по финалу. Что прорабатываем:

1. **Персистентность кредов** между пересозданиями контейнера. Направление:
   ТОЧЕЧНЫЕ named volumes на секрет-каталоги (`~/.claude` + `~/.claude.json`,
   `~/.npmrc`, `~/.config/gh`, `~/.gitconfig`) вместо volume на весь home —
   home остаётся cattle, секреты переживают пересоздание. Вопрос к прогону brainer:
   что именно из `~/.claude*` должно жить (config dir vs state).
2. **Git-идентичность**: `.gitconfig` в том же секрет-volume; подпись коммитов —
   не вводим сейчас (нет требования), задел: ssh-agent-проброс.
3. **Harness-хуки (.claude репо)**: живут в bind-mount рабочей копии — должны
   работать как есть; проверка — прогон brainer (claude-scope, git-gate).
4. **Docker-демон из сессии** (управление стеками): socket-проброс в devbox —
   НЕ дефолт (socket = root хост-докера; агент с ним = полный доступ к машине).
   Отдельный «ops-профиль» devcontainer'а с явным mount'ом — включается осознанно,
   для architect-сессий devopser. Безопасность обсудить с оракулом при показе.
5. **Порты**: forwardPorts по потребности репо (расширение шаблона потребителем).
