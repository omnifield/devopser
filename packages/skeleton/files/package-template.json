{
  "name": "__NAME__",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.11.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prepare": "husky",
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "typecheck": "nx run-many -t typecheck",
    "lint": "nx run-many -t lint",
    "affected": "nx affected -t lint,typecheck,test,build",
    "hygiene": "sherif"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@nx/js": "^23.0.1",
    "@omnifield/biome-preset": "__PRESET_VERSION__",
    "@omnifield/nx-preset": "__PRESET_VERSION__",
    "husky": "^9.1.7",
    "nx": "^23.0.1",
    "sherif": "^1.13.0",
    "typescript": "^5.9.3"
  }
}
