# GENERATED by hub-core (devopser/hub-core/generate.mjs) — НЕ править руками.
# Источник — omnifield.yaml продуктов воркспейса. Регенерация: pnpm -C hub-core generate.
# Single-origin: наружу только :8080, апстримы по docker-сети omnifield-gateway
# (alias = имя продукта). resolver → runtime-резолв (gateway стартует без продуктов).

resolver 127.0.0.11 valid=10s ipv6=off;

# ws/HMR-safe: upgrade на ws-запрос, close иначе (vite HMR + обычные/SSE).
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;
    charset utf-8;

    root /usr/share/nginx/html;
    index index.html;

    # brainer (fullstack) → brainer:3500
    location /brainer {
        set $up_brainer_3500 brainer;
        proxy_pass http://$up_brainer_3500:3500;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 1h;
    }

    # brainer (fullstack) → brainer:8010
    location /api/brainer {
        set $up_brainer_8010 brainer;
        proxy_pass http://$up_brainer_8010:8010;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 1h;
    }

    # weber (frontend) → weber:5173
    location /weber {
        set $up_weber_5173 weber;
        proxy_pass http://$up_weber_5173:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 1h;
    }
}
